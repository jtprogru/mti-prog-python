# QWEN.md — Контекст для работы с проектом

## Обзор проекта

**mti-prog-python** — это коллекция учебных лабораторных работ по программированию на Python. Репозиторий содержит набор независимых скриптов, демонстрирующих базовые возможности языка: работу с типами данных, ввод/вывод, арифметические операции, строки, структуры данных, файловую систему и ООП.

Проект предназначен для образовательных целей и не требует сборки или тестирования. Каждый скрипт является самодостаточным модулем с функцией `main()` и может запускаться напрямую через интерпретатор Python.

---

## Технологии и зависимости

- **Язык:** Python 3.14 (требуется версия ≥3.14)
- **Менеджер пакетов:** [uv](https://docs.astral.sh/uv/)
- **Виртуальное окружение:** `.venv` (создаётся через `uv venv` или `uv sync`)
- **Внешние зависимости:** отсутствуют (используется только стандартная библиотека)
- **Dev-зависимости:** `ruff`, `mypy` (опционально, для линтинга и проверки типов)

---

## Запуск скриптов

### Через uv (рекомендуется)

```bash
# Синхронизировать окружение
uv sync

# Запустить скрипт
uv run labs lab1
uv run labs calculator

# Запустить dev-инструменты
uv run ruff check labs/
uv run mypy labs/
```

### Базовый запуск (без uv)

```bash
python labs/<скрипт>.py
```

Примеры:
```bash
python labs/lab1.py
python labs/calculator.py
python labs/diary_app.py
```

### Работа в виртуальном окружении

```bash
# Создание через uv
uv venv

# Активация (macOS/Linux)
source .venv/bin/activate

# Запуск скрипта
python labs/<скрипт>.py

# Деактивация
deactivate
```

---

## Список скриптов

| Файл | Описание |
|------|----------|
| `labs/main.py` | Точка входа, приветствие |
| `labs/lab1.py` | Демонстрация типов данных, арифметики, ввода пользователя |
| `labs/calculator.py` | Простой калькулятор (+, -, *, /) |
| `labs/anketa.py` | Сбор анкеты пользователя, вывод таблицы |
| `labs/triangle.py` | Вычисление площади и периметра треугольника |
| `labs/multiplication_table.py` | Таблица умножения |
| `labs/sentence_analysis.py` | Анализ предложения (слова, символы) |
| `labs/student_grades.py` | Учёт оценок студентов |
| `labs/inventory.py` | Работа с инвентарём |
| `labs/library_system.py` | Симуляция библиотечной системы (классы, JSON, даты) |
| `labs/main_lab3.py` | Дополнительная лабораторная работа |
| `labs/string_operations.py` | Операции со строками |
| `labs/diary_app.py` | Приложение-ежедневник (CRUD, JSON, экспорт) |

---

## Архитектура и соглашения

### Структура скриптов

Каждый файл следует единому шаблону:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-

def main():
    # Основная логика
    pass

if __name__ == "__main__":
    main()
```

### Кодировка

Все файлы используют UTF-8 (указано в shebang-комментарии).

### Стиль кода

- Имена переменных: `snake_case`
- Строки документации: краткие комментарии или docstrings
- Обработка ошибок: `try/except` с валидацией ввода
- Интерактивность: `input()` для взаимодействия с пользователем

### Хранение данных

Некоторые скрипты (`diary_app.py`, `library_system.py`) используют JSON-файлы для сохранения состояния между запусками:
- `data/tasks.json` — задачи ежедневника
- `data/library_data.json` — данные библиотеки

---

## Расширение проекта

### Добавление нового скрипта

1. Создайте файл `labs/<имя>.py`
2. Добавьте shebang и кодировку:
   ```python
   #!/usr/bin/env python3
   # -*- coding: utf-8 -*-
   ```
3. Реализуйте функцию `main()` с логикой
4. Добавьте блок `if __name__ == "__main__": main()`
5. Обновите `README.md` и `QWEN.md`, добавив скрипт в таблицу

### Добавление зависимостей через uv

```bash
# Добавить основную зависимость
uv add <пакет>

# Добавить dev-зависимость (линтеры, тесты)
uv add --dev <пакет>
```

### Добавление тестов (опционально)

При необходимости можно добавить тесты:

```bash
mkdir tests
# Создать test_<скрипт>.py с использованием unittest или pytest
```

### Линтинг и форматирование

Проект использует `ruff` и `mypy` в качестве dev-зависимостей:

```bash
# Проверка кода
uv run ruff check labs/

# Проверка типов
uv run mypy labs/

# Автоформатирование
uv run ruff format labs/
```

---

## Примечания для Qwen Code

- Все скрипты независимы, нет общего состояния или базы данных
- Проект использует `uv` для управления зависимостями
- Запуск через `uv run labs <скрипт>` или `python labs/<файл>.py`
- При модификации кода сохраняйте существующий стиль (shebang, кодировка, `main()`)
- Не добавляйте внешние зависимости без явного запроса пользователя
- При создании новых файлов используйте UTF-8 и `snake_case` для имён
- Новые скрипты создаются в директории `labs/`

---

## Ссылки

- [README.md](README.md) — основная документация проекта
- [CLAUDE.md](CLAUDE.md) — руководство для Claude Code
- `.python-version` — требуемая версия Python (3.14)
- [Документация uv](https://docs.astral.sh/uv/) — менеджер пакетов и виртуальных окружений
